# Example environment variables
# Copy to .env.local for local development

# GitHub (optional for local scripts that hit GitHub APIs)
GITHUB_TOKEN=github_pat_example
GITHUB_OWNER=sree-pm
GITHUB_REPO=infonaut-ltd
GITHUB_BRANCH=main

# NextAuth (GitHub OAuth)
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace_with_random_string

# Optional: set the template repo for admin fork verification
TEMPLATE_REPO_OWNER=sree-pm
TEMPLATE_REPO_NAME=infonaut-ltd
# GitHub Configuration
# Get token from: https://github.com/settings/tokens
# Scopes needed: repo (all), read:user
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxx
GITHUB_OWNER=sree-pm
GITHUB_REPO=infonaut-ltd
GITHUB_BRANCH=main

# GitHub OAuth (for editor login)
# Create an OAuth App in GitHub: https://github.com/settings/developers
# Set Authorization callback URL to: https://your-site.com/api/auth/callback or http://localhost:3000/api/auth/callback
GITHUB_OAUTH_CLIENT_ID=your_github_oauth_client_id
GITHUB_OAUTH_CLIENT_SECRET=your_github_oauth_client_secret
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Template repo (must be marked as a template on GitHub)
TEMPLATE_OWNER=sree-pm
TEMPLATE_REPO=infonaut-ltd

# Cloudflare Pages Configuration
# Get from Cloudflare Pages project settings
CLOUDFLARE_API_TOKEN=your_api_token_here
CLOUDFLARE_ACCOUNT_ID=your_account_id_here

# Optional: API endpoint base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
